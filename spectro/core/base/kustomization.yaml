namePrefix: capv-

commonLabels:
  cluster.x-k8s.io/provider: "infrastructure-vsphere"

resources:
  - ../../../config/rbac
  - ../../../config/manager

patchesStrategicMerge:
  - ../../../config/base/manager_image_patch.yaml
  - ../../../config/base/manager_pull_policy.yaml

patchesJson6902:
  - target:
      group: apps
      kind: Deployment
      name: controller-manager
      namespace: system
      version: v1
    path: patch_healthcheck.yaml
  - target:
      group: apps
      kind: Deployment
      name: controller-manager
      namespace: system
      version: v1
    path: patch_deployment_service_account_name.yaml
  - target:
      group: rbac.authorization.k8s.io
      kind: ClusterRoleBinding
      name: manager-rolebinding
      version: v1
    path: patch_rbac_service_account_name.yaml
  - target:
      group: rbac.authorization.k8s.io
      kind: RoleBinding
      name: leader-election-rolebinding
      version: v1
    path: patch_rbac_service_account_name.yaml
